PORT=3333

# Banco de Dados MySQL
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASS=SUA_SENHA_AQUI
DB_NAME=calculadora_escolar_db

# JWT — gere uma chave forte, ex: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=GERE_UMA_CHAVE_SECRETA_FORTE_AQUI
JWT_EXPIRES_IN=1d
